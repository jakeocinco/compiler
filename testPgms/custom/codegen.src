program LoopFib is
    variable test_arr : string[4];
    variable loop_val : integer;
    variable tempBool : bool;
begin
    loop_val := 0;
    test_arr[loop_val] := 1;
    test_arr[1] := 2;
    test_arr[2] := 4;
    test_arr[3] := 16;
    for (loop_val := 0; loop_val < 4)
        tempBool := putInteger(test_arr[loop_val]);
        loop_val := loop_val + 1;
    end for;
    //for (loop_val := 0; loop_val < 4)
    //    tempBool := putInteger(test_arr[
end program.
