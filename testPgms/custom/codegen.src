program LoopFib is
    variable loopVal : string;
    variable tempBool : bool;
begin
    if ( true ) then
        loopVal := "true";
    else
        loopVal := "false";
    end if;
    tempBool := putString(loopVal);
end program.
