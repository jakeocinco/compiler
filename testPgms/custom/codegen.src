program LoopFib is
    variable test : string;
    variable tempBool : bool;
begin
    test := getString();
    tempBool := putString(test);
    test := getString();
    tempBool := putString(test);
end program.
